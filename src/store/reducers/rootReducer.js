//state: trang thai redux
const initState = {
    users: [
        {id: 1, name: 'Chi'},
        {id: 2, name :'Thoi'}
    ]
}
const rootReducer = (state = initState, action) => {

    return state;
}
export default rootReducer;